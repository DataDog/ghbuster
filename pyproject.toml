[project]
name = "ghbuster"
version = "0.1.0"
description = "A tool to identify and investigate inauthentic GitHub user accounts and repositories"
requires-python = ">=3.10"
dependencies = [
    "networkx[default]>=3.6.1",
    "pygithub>=2.9.0",
    "pyvis>=0.3.2",
    "requests-cache>=1.3.1",
]

[project.scripts]
ghbuster = "ghbuster.__main__:cli_entrypoint"
